{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    <h1>La page de recherche d'espèces</h1>

    <table class="table">
        <tr>
            <th>Nom</th>
            <th>Nom Scientifique</th>
            <th>Ordre</th>
            <th>Famille</th>
        </tr>
        <tr>
        {% for species in listSpecies %}
            <td>{{ species.name }}</td>
            <td>{{ species.scientificName }}</td>
            <td>{{ species.sequence }}</td>
            <td>{{ species.family }}</td>
        {% endfor %}
        </tr>
    </table>

{% endblock %}